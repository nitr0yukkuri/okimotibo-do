apiVersion: v1
kind: Service
metadata:
  name: okimochi-backend
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: okimochi-backend
  ports:
    - name: http
      port: 8080
      targetPort: http
