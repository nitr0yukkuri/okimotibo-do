apiVersion: v1
kind: Service
metadata:
  name: okimochi-redis
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: okimochi-redis
  ports:
    - name: redis
      port: 6379
      targetPort: redis
